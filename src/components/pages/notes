how to push to HeroKu

Open app.py, close server

go to HeroKu
create new app, name whatever, create app

grab line for Existing Git Repository
enter that line into vs code terminal

then enter these 2 lines one at a time
pipenv install gunicorn psycopg2

open procfile named as "Procfile" 
in file:
web: gunicorn app:app
